{
  "ignorePatterns": [
    { "pattern": "^http://localhost" },
    { "pattern": "^https://your-handle" },
    { "pattern": "^https://<" }
  ],
  "timeout": "20s",
  "retryOn429": true,
  "retryCount": 3,
  "fallbackRetryDelay": "30s"
}
